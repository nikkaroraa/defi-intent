{
  "name": "@defi-intent/protocol-adapters",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@defi-intent/shared": "workspace:*",
    "viem": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
